{% extends'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Patienten toevoegen</h1>

                {{ form_start(patientForm, {'attr': {'novalidate': 'novatidate'}}) }}
                {{ form_widget(patientForm) }}
                {{ form_end(patientForm) }}
            </div>
        </div>
    </div>
{% endblock %}