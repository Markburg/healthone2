{% extends'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Afdeling Toevoegen</h1>

                {{ form_start(afdelingForm, {'attr': {'novalidate': 'novatidate'}}) }}
                {{ form_widget(afdelingForm) }}

                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(afdelingForm) }}
            </div>
        </div>
    </div>
{% endblock %}